// The default location for a meta file is the module folder and the filename "meta.cfg"

cliHelp = {
	options = ( // list of CLI options help
		{
			module = "module name" // the name of the module
			short = "c" // the short option, optional if "long" provided
			long = "config" // the long option, optional if "short" provided
			help = "some help." // the help text
		}
	)
	arguments = ( // list of CLI argument help
		{
			name = "test" // the name of the argument
			module = "me" // the module name
			help = "huhu" // the help text
		}
	)
}
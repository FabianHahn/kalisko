cliHelp = {
	options = (
		{
			module = "config"
			short = "c"
			long = "config"
			help = "File path to a config file. Standard config files will be not loaded (except the writable one)."
		}
	)
}
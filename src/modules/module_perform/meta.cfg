cliHelp = {
	options = (
		{
			module = "module_perform"
			short = "m"
			long = "load-modules"
			help = "Comma separated list of module names to load. Config will be ignored."
		},
		{
			module = "module_perform"
			long = "append-modules"
			help = "Comma separated list of module names to load after loading the modules given by the config."
		}
	)
}